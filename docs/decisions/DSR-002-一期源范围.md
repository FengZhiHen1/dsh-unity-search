# DSR-002 一期源范围

> 决定：一期 = 网页引擎链（10 引擎）+ 学术 5 源（arXiv/OpenAlex/Crossref/PubMed/EuropePMC）+ 平台 8 源 + 有界阅读；学术长尾经 L3 桥，原生化延期二期。
> 理由：覆盖"统一多源"的完整故事且验证面可控；引文功能依赖学术源先稳定。
> 被否决项：仅网页链 + 学术 3 源（故事不完整）；全量一次到位（验证面过大）；学术长尾一期全做原生适配（多数经 L3 桥今天可用，google_scholar 反爬实测 403 不宜原生）。
>
> 修订（2026-09-09 源覆盖审计后用户指令）：初版"8 引擎 + 学术 3 源 + 平台 5 源"扩容为"10 引擎 + 学术 5 源 + 平台 8 源"，详见「修订记录」。

## 上下文

RQ-02 要求多源统一检索。候选源族：网页引擎（L0 自包含自带）、学术源、平台源、有界阅读；差异化候选：引文图谱（OpenAlex 引文边）与 BibTeX 导出（五仓库模型面全缺，Crossref 原生支持 CSL/BibTeX 端点）。

## 方向与评价

| 方向 | 何时成立 | 代价与风险 |
| --- | --- | --- |
| 网页链 + 学术 3 源 | 最小差异化、最快过门禁 | 平台源缺席，"统一多源"故事不完整；read_source 缺席则"获取资料"半空 |
| 网页链 + 学术 3 源 + 平台 5 源 + 有界阅读 | 完整覆盖 RQ-02/RQ-04；平台源全是稳定 JSON API，适配器成本低 | 适配器数量 16 个，测试面扩大 |
| 全量一次到位 | 用户不介意工作量 | `get_bibliography` 依赖学术源先稳定；一期验收面过宽，门禁周期拉长 |

评价维度：需求覆盖、验证成本、依赖顺序、二期跟进成本。

## 决定与理由

选中间方案。平台 5 源（GitHub/StackOverflow/HN/Wikipedia/npm）全是 keyless 稳定 JSON API，边际成本低而"统一"收益完整；有界阅读是 RQ-04 的唯一载体，不能延期。引文/BibTeX 与学术二期源延期：端点成本低但依赖学术源实测稳定，且工具面预算（C-01 ≤3）的第 3 个槽位为其保留。

## 直接后果

- 波及文档：需求.md（RQ-02/RQ-04 与非目标）、technical-details/源适配器清单与端点契约.md（16 源清单）、technical-details/工具面与有界阅读.md（工具面）。
- 二期项登记于 TODO.md（含重访条件）。

## 重访条件

- 学术五源在 test 实测中表现不稳定（如 Crossref 限速频繁触发）时，二期源的优先级重排。
- 平台源中任一 API 政策收紧（如 GitHub 匿名配额下调、Bilibili 反爬升级）时，该源降级为可选 key 源并重评清单。

## 修订记录

**2026-09-09 源覆盖审计扩容（用户指令）**。审计对照用户提供的 paper-search skill（paper-search-mcp CLI 0.1.4，21 源实测矩阵）与五个参考项目的全源清单，发现初版三处缺口：

1. **修正级**：`deepseek-official` 未收进链——宿主 web 行现任 provider 即它，本插件 pin 走 seam 后不收等于用户现有能力回退。收进链内 key 引擎首位（默认启用，`available()` 凭据缺席自动降级）。
2. **扩容**：`keenable`（keyless 公共 MCP 端点）引擎；平台 `v2ex`/`bilibili`/`reddit`（free-search 八平台剩余三个，初版决策未单独讨论取舍）；学术 `pubmed`/`europepmc`（paper-search skill 2026-09-08 实测稳定源）。
3. **审计同时确认不扩**：学术长尾 13 源（semantic/unpaywall/doaj/openaire/biorxiv/medrxiv/pmc/iacr/dblp/zenodo/hal/core）维持 L3 桥 + 二期登记；google_scholar/ssrn/base/citeseerx 建议不做原生（实测反爬 403/常空）；Sci-Hub 仍为非目标。

适配器总数 16 → 23。波及文档：需求.md（RQ-02/AC-02 与范围说明）、technical-details/源适配器清单与端点契约.md（23 源清单与端点）、technical-details/设置凭据与Skill.md（settings yaml）、项目结构设计.md（适配器目录）、TODO.md（二期清单补全）、借鉴地图.md（新端点出处）。
