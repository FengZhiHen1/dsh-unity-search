# 源适配器清单与端点契约

本文唯一拥有：一期 23 个源适配器的端点、认证、限速、解析要点与已知脆弱性（2026-09-09 源覆盖审计后自 16 扩容，DSR-002 修订记录）。适配器接口与信封归「检索核心与信封契约」；引擎开关与 key 引用名归「设置凭据与Skill」。

## 总览

| id | family | 认证 | timeRange | 已知脆弱性 |
| --- | --- | --- | --- | --- |
| bing | web | 无 | **不支持**（ez 参数实证无效，2026-09-09） | HTML 抓取，结构随上游改版漂移 |
| ddg | web | 无 | 参数支持（`df`，本机被墙未证效） | 同上；反爬限流敏感 |
| ddg-lite | web | 无 | 参数支持（`df`，同上未证效） | 同上，输出更简 |
| anysearch | web | 无 | 不支持 | JSON 结果列表（活体证实响应无 answer 字段） |
| searxng | web | 无 | 参数支持（`time_range`，公共实例本机不可达未证效） | 公共实例稳定性不一，需配置实例 |
| tavily | web | key | 参数支持（API 文档语义，未持 key 实跑） | 免费额度有限 |
| exa | web | key | 参数支持（`startPublishedDate`，未实跑） | 同上 |
| perplexity | web | key | 不支持 | 答案型，citations 可能仅 URL |
| deepseek-official | web | key | 不支持 | 答案型（Anthropic 兼容端点 + 内置 web_search 工具）；宿主现任 provider，收进链防能力回退 |
| keenable | web | 可选 key | **✓ 实证有效**（publishedAfter 窗内命中） | 无 key 走公共 MCP 端点，文本抽取较脆弱 |
| arxiv | academic | 无 | **✓ 实证有效**（submittedDate 区间） | 仅摘要级元数据；约 3s 间隔要求 |
| openalex | academic | 无（mailto 礼貌池） | 参数支持（已发未逐条断言） | 元数据覆盖全，非全文 |
| crossref | academic | 无（mailto 礼貌池） | 参数支持（已发未逐条断言） | 同上 |
| pubmed | academic | 无 | 参数支持（已发未逐条断言） | 仅题录/摘要；NCBI 匿名约 3 req/s |
| europepmc | academic | 无 | 参数支持（已发未逐条断言） | 题录 + OA 标识；与 PMC 重叠 |
| github | platform | 可选 token 提额 | 不支持 | 匿名 60 req/h |
| stackoverflow | platform | 无 | 不支持 | 配额按 IP |
| hn | platform | 无 | **✓ 实证有效**（numericFilters） | Algolia 镜像，略滞后于主站 |
| wikipedia | platform | 无 | 不支持 | — |
| npm | platform | 无 | 不支持 | — |
| v2ex | platform | 无 | 不支持 | 非真搜索：热榜本地过滤，覆盖面有限 |
| bilibili | platform | 无 | 不支持 | 公开接口，反爬策略可能收紧 |
| reddit | platform | 无 | 粗（`t=day` 级 + 动态 uncertainty，未证效） | old.reddit JSON；UA 须带 contact 尾注 |

## 网页引擎（family: web）

| id | 端点与要点 |
| --- | --- |
| bing | `GET https://www.bing.com/search?q=…&count=…`；HTML 解析结果块（标题/链接/摘要），解析锚点 `li.b_algo` 活体证实稳定；`timeRange` 不实现——`filters=ex1:"ezN"` 活体实证对结果集零影响（基线/day/week/month 四组同结果，含长尾查询复测），已从实现中删除 |
| ddg | `GET https://html.duckduckgo.com/html/?q=…`；HTML 解析；`timeRange` 映射 `df=d|w|m|y`（参数构造正确；本机 DNS 污染致不可达，效果未证） |
| ddg-lite | `GET https://lite.duckduckgo.com/lite/?q=…`；极简 HTML，解析容错优先；`timeRange` 同映 `df`（同上未证效） |
| anysearch | `POST https://api.anysearch.com/v1/search`；JSON 返回 `data.results[] = {title,url,snippet,content}`（活体 2026-09-09 证实）；**响应无 `answer` 字段**——实现保留宽松透传（有则进信封，无则 null）；snippet 缺时回落 content |
| searxng | `GET {instance}/search?q=…&format=json`；实例列表来自 settings（公共实例参考 searx.be、searx.tiekoetter.com、search.inetol.net，默认空即不启用；2026-09-09 本机测 5 公共实例全 connect 超时——DNS 污染所致，换网络复测） |
| tavily | `POST https://api.tavily.com/search`；`Authorization: Bearer <key>`；返回 answer + results |
| exa | `POST https://api.exa.ai/search`；key 认证；支持 `numResults` 与日期过滤 |
| perplexity | `POST https://api.perplexity.ai/chat/completions`；答案在 message、citations 单独数组（可能仅 URL，信封 `items` 允许缺 title/snippet） |
| deepseek-official | `POST https://api.deepseek.com/anthropic/v1/messages`（Anthropic 兼容）；`x-api-key` + `Authorization: Bearer` + `anthropic-version: 2023-06-01`；body `tools: [{type: "web_search_20250305", name: "web_search", max_uses: 1}]`、model `deepseek-v4-flash`；解析 `web_search_tool_result` 块内 `web_search_result`（url/title/page_age），text 块 citations 的 `cited_text` 作 snippet；401 = key 失效 |
| keenable | 无 key：`POST https://api.keenable.ai/mcp`（MCP JSON-RPC `tools/call` → `search_web_pages`，取 `result.content[].text` 按 `Title:/URL:/Published:/Snippets:` 锚点抽取——文本块形态活体证实与实现一致，`Published` 可缺省、`Acquired` 并存；`result.isError` 为业务失败）；有 key：`POST https://api.keenable.ai/v1/search`（`KEENABLE_API_KEY`，额度更高）；双形态按凭据解析结果自动选择；`timeRange` → `publishedAfter` 相对词汇（`12h/Nd/Nmo/Ny`）**实证窗内命中** |

端点知识来源：free-search 运行中代码（`references/open-source/dsh-free-search`）与 modsearch 调研记录；契约可抄、代码不搬。首轮活体验证结论见下节《活体验证记录》。

## 活体验证记录（2026-09-09，本机直连）

探针 = 一次性脚本直接驱动 `src/core` 真实实现（真 fetch / 真 DNS / resolveConfig 默认值），关键结论：

- **实跑成功（12）**：bing（`b_algo` 锚点稳定，5 条 402ms）、anysearch（键树取证修正 answer 预期）、keenable（MCP 文本块锚点与解析一致；week 窗 5/5 命中窗内）、arxiv（submittedDate 12 位戳生效、3s 节流下正常）、openalex、crossref、pubmed（两步链）、stackoverflow、hn（numericFilters epoch 界精确）、npm、bilibili（-412 未触发）、web 引擎链整跑（bing 胜出 1789ms，degraded 注记路径正确）。
- **实现级修正（3）**：europepmc 响应形状 `resultList.result`（原误 `hits.hitList` → parse_failed 实锤后修复复跑通过）；bing `filters=ex1:"ezN"` 判零效果 → 删映射并 `supportsTimeRange: false`；anysearch `content` 作 snippet 回落。
- **read_source 冒烟（3/3）**：example.com 抽取 + artifact 落盘；arxiv abs 页 3096 字符抽取（文件名 `20260909-110926-arxiv.org-c29fa68b.txt` 形态契约吻合）；`169.254.169.254` → `unsafe_target` 拒绝（SSRF 活体验证）。
- **本机网络不可证（7 源 + searxng 全实例）**：ddg / ddg-lite / wikipedia / v2ex / reddit / github / searxng——分两层成因：**① hosts 劫持**（Steam++ 写入 `C:\Windows\System32\drivers\etc\hosts`：`api.github.com`/`github.com` 等 → `127.0.0.1`，Node 默认解析走 hosts → 连到本机 443 服务出现 `UNABLE_TO_VERIFY_LEAF`；同 hosts 块还含 steam/youtube/huggingface）；**② 网络层 DNS 污染**（ddg/wikipedia/reddit/v2ex 不在 hosts，本地解析非确定性返回经典伪造答案池 IP 或超时：`html.duckduckgo.com → 157.240.1.9`、`zh.wikipedia.org → 31.13.94.10`（Facebook 段）、`old.reddit.com → 104.244.42.197`（Twitter 段）、`www.v2ex.com → EAI_AGAIN`；对照组阿里 DoH 亦返回同池假值（duckduckgo→108.160.170.43 等）——污染在更上游，DoH 只保护最后一公里）。系统 DNS 为校园网递归 `202.118.66.6`。直连对照与插件跑签名一致 → **实现未判负，请求构造与出站参数经 out-bound 记录核实正确；换净网络/代理 TUN 复测**。
- **bing 幸存解释**：`www.bing.com` 走微软国内 CDN（`china.bing123.com → 202.89.233.x`），本地解析与 DoH 一致，不在伪造名单 → 引擎链默认首选在本环境实测可用。
- **SKIP（4 key 引擎）**：tavily / exa / perplexity / deepseek-official 未持 key（探针不读 DSH 凭据中心）；参数构造按 API 文档实现，持 key 环境复测。
- **bing 时间窗证据**：`q=大语言模型 最新进展` 与长尾 `q=python asyncio tutorial` 各跑 none/ez1/ez2/ez3 四组——host 集逐组相同 → filters 参数在 www.bing.com（zh-CN market）不生效。

## 学术源（family: academic）

| id | 端点与要点 |
| --- | --- |
| arxiv | `GET http://export.arxiv.org/api/query?search_query=all:…&start=0&max_results=…&sortBy=submittedDate`；Atom XML 解析（`entry`：title/id/summary/published/authors）；`sourceIds.arxiv` 取 id 尾段；条款要求约 3s 间隔（core 限速闸落实） |
| openalex | `GET https://api.openalex.org/works?search=…&per-page=…&mailto={contact}`；JSON；`sourceIds.doi` 取 `doi` 字段（去 `https://doi.org/` 前缀）；`extra`：`authorships` 取作者、`primary_location.source.display_name` 作 venue、`cited_by_count`；`timeRange` 映射 `filter=from_publication_date:` |
| crossref | `GET https://api.crossref.org/works?query=…&rows=…&mailto={contact}`；UA 带 contact 进礼貌池；`sourceIds.doi` 取 `DOI`；`extra`：`author`、`container-title`、`is-referenced-by-count` |
| pubmed | 两步：`GET https://eutils.ncbi.nlm.nih.gov/entrez/eutils/esearch.fcgi?db=pubmed&term=…&retmode=json&retmax=…` 得 PMID 列表，`efetch.fcgi?db=pubmed&id=…&retmode=xml` 取题录（XML 解析）；url = `https://pubmed.ncbi.nlm.nih.gov/{pmid}/`；`sourceIds.pmid`；NCBI 匿名约 3 req/s（core 限速闸 1000ms 满足），条款建议带 `tool`/`email` 参数（复用 `contact`） |
| europepmc | `GET https://www.ebi.ac.uk/europepmc/webservices/rest/search?query=…&format=json&pageSize=…&resultType=core`；JSON 结果路径 = **`resultList.result[]`**（活体 2026-09-09 修正，首版误写 `hits.hitList` 已修）；条目无 `pubDate`，日期取 `firstPublicationDate` → `pubYear` 兜底；`sourceIds.doi`/`pmid`；`extra`：`isOpenAccess`（Y/N）/`pmcid`/`journalInfo`；landing url 优先级 DOI > PMC > PMID；OA 全文/PDF 存在性入 `extra`，不自动抓取（有界原则） |

去重跨源主键 = DOI → arXiv id → 规范化 URL → 标题+首作者（规则归「检索核心与信封契约」）；pubmed 与 europepmc 的重叠条目经 DOI 或规范化 URL（`pubmed.ncbi.nlm.nih.gov/{pmid}/`）键在去重层合并。学术条目 `uncertainty` 恒定标注"仅元数据/摘要级"。

## 平台源（family: platform）

| id | 端点与要点 |
| --- | --- |
| github | `GET https://api.github.com/search/repositories?q=…&per_page=…`；匿名 60 req/h，配 `apiKeyEnv` token 提额；`extra`：`stargazers_count`/`language`/`updated_at` |
| stackoverflow | `GET https://api.stackexchange.com/2.3/search/advanced?site=stackoverflow&q=…&pagesize=…`；`extra`：`score`/`answer_count`/`is_answered` |
| hn | `GET https://hn.algolia.com/api/v1/search?query=…&hitsPerPage=…`（`search_by_date` 变体）；`extra`：`points`/`num_comments` |
| wikipedia | `GET https://{lang}.wikipedia.org/w/api.php?action=query&list=search&srsearch=…&format=json`；`lang` 来自 settings（默认 `zh`）；snippet 含 HTML 标记需剥除 |
| npm | `GET https://registry.npmjs.com/-/v1/search?text=…&size=…`；`extra`：`version`/`date` |
| v2ex | `GET https://www.v2ex.com/api/topics/hot.json` 拉热榜后本地过滤（title/content 含 query）——**非真搜索**，覆盖面限于当期热榜；条目 `uncertainty` 恒定标注"仅热榜过滤"；url = `https://www.v2ex.com/t/{id}`；snippet 取 content 截断 |
| bilibili | `GET https://api.bilibili.com/x/web-interface/search/all/v2?keyword=…`；headers 必须带 `referer: https://www.bilibili.com`；`data.code !== 0` 为业务错误；遍历 `data.result[].data[]` 取 `arcurl`/`title`（剥 HTML 标签）/`desc` |
| reddit | `GET https://old.reddit.com/search.json?q=…&limit=…&sort=relevance`；UA 带 contact 尾注（Reddit 条款要求）；`data.data.children[].data` 取 `url`/`title`/`selftext` |

## 失败语义

- 所有适配器把上游非 2xx、超时、解析失败归一为 `SourceOutcome.error`（code 词汇表归「检索核心与信封契约」），不抛出。
- 解析失败的判定：HTML/JSON 结构不含任何可识别结果块；空结果（结构正常、零条目）不是失败。
- 抓取类引擎（bing/ddg/ddg-lite）改版导致解析失效率高时，处置 = 引擎自然冷却 + 修复解析锚点发版；不做运行时自动切换实现。
